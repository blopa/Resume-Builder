// Shared colour helpers so every Compact section resolves the same palette.
export const mutedColor = (theme) => (theme.palette.type === 'dark' ? '#a8a8a8' : '#6b6b6b');

export const ruleColor = (theme) => (theme.palette.type === 'dark' ? '#4a4a4a' : '#dcdcdc');
