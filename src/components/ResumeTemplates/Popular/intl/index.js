// The Popular template uses the same section vocabulary as Compact. Keeping the
// message catalogue shared prevents identical translations from drifting.
export { default } from '../../Compact/intl';
